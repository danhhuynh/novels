# name: Deploy Web to Lightsail

# on:
#   push:
#     paths:
#       - 'services/web/**'
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Zip web service
#         run: |
#           cd services/web
#           zip -r ../../web-deploy.zip .

#       - name: Upload to Lightsail
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.LIGHTSAIL_HOST }}
#           username: ${{ secrets.LIGHTSAIL_USER }}
#           key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
#           source: "web-deploy.zip"
#           target: "~/"

#       - name: Deploy on Lightsail
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.LIGHTSAIL_HOST }}
#           username: ${{ secrets.LIGHTSAIL_USER }}
#           key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
#           script: |
#             # Ensure required tools are installed
#             command -v unzip >/dev/null 2>&1 || sudo apt-get update && sudo apt-get install -y unzip
#             command -v npm >/dev/null 2>&1 || curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash - && sudo apt-get install -y nodejs
#             command -v pm2 >/dev/null 2>&1 || sudo npm install -g pm2

#             # Prepare deploy directory
#             sudo mkdir -p /opt/novels/services/web
#             sudo unzip -o ~/web-deploy.zip -d /opt/novels/services/web
#             cd /opt/novels/services/web

#             npm install --production
#             npm run build

#             pm2 restart novels-web || pm2 start server.js --name novels-web
